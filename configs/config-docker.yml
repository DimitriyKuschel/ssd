pidFile: "/tmp/ssd.pid"
statistic:
  interval: 60
webServer:
  host: "0.0.0.0"
  port: 8090
persistence:
  filePath: "/data/ssd/data.bin"
  saveInterval: 120
logger:
  level: "info"
  mode: 0640
  dir: "/var/log/ssd"
