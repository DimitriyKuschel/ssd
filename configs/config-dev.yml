pidFile: "/tmp/ssd.pid"
statistic:
  interval: 60s
webServer:
  host: "127.0.0.1"
  port: 8090
persistence:
  filePath: "/etc/ssd/data.bin"
  saveInterval: 120s
cache:
  enabled: true
  size: 32
logger:
  level: "info"
  mode: 0640
  dir: "/var/log/ssd"
